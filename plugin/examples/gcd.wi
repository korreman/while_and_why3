a b c d t;

t <- 0;

a <- 20;
b <- 15;

c <- a;
d <- b;

while not b = 0 invariant true do
    t <- b;
    b <- a % b;
    a <- t;
end;

assert
    forall q.
        c % q = 0 /\
        d % q = 0
    ->
        a <= q;
